# Owner Studio is enabled automatically during local development.
# Do not enable it on a public production site without replacing browser-only
# storage and the client-side passcode with real server authentication.
VITE_ENABLE_OWNER_STUDIO=false
VITE_OWNER_PASSCODE=change-this-local-owner-passcode
